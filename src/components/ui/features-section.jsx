import { FeaturesSectionWithCardGradient } from './features-card-gradient'

const FeaturesSection = ({ grid, gridHeading }) => {
  return (
    <FeaturesSectionWithCardGradient grid={grid} gridHeading={gridHeading} />
  )
}

export default FeaturesSection
